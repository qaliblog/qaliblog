<div class="row">
  <h1 class="hart col-md-7"><%= t('views.contact.contactustitle') %></h1>
  <% if @contacts.any? %>
    <div class="col-md-5 hart">
      <%= link_to t('views.contact.massages'), contacts_path, class: "btn btn-success" %>
    </div>
  <% end %>
</div>
<%= render 'form' %>
